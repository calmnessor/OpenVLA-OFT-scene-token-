# 新服务器迁移指南

## 目录

1. [需要上传的文件](#1-需要上传的文件)
2. [Conda 环境配置](#2-conda-环境配置)
3. [RLBench + CoppeliaSim 安装](#3-rlbench--coppeliasim-安装)
4. [Headless 渲染修复](#4-headless-渲染修复)
5. [数据收集](#5-数据收集)
6. [数据转换（HDF5 → RLDS）](#6-数据转换hdf5--rlds)
7. [模型训练](#7-模型训练)
8. [离线 LoRA 合并](#8-离线-lora-合并)
9. [模型评估](#9-模型评估)
10. [常见问题](#10-常见问题)

---

## 1. 需要上传的文件

### 1.1 项目代码（必须）

将整个 `openvla-oft/` 目录上传到服务器，建议路径 `/opt/openvla-oft/`：

```bash
# 本地打包上传
tar czf openvla-oft.tar.gz openvla-oft/
scp -P <port> openvla-oft.tar.gz root@<host>:/opt/
ssh root@<host> -p <port>
cd /opt && tar xzf openvla-oft.tar.gz
```

### 1.2 已训练的 LoRA 适配器（必须）

从 `saved_from_server/` 上传 close_jar 的 LoRA 权重：

```
saved_from_server/runs/openvla-7b+rlbench_close_jar+b4+lr-2e-05+lora-r32+dropout-0.0--image_aug--250_chkpt/lora_adapter/
├── adapter_model.safetensors   (675 MB)
└── adapter_config.json
```

上传到服务器的 `/root/checkpoints/close_jar_lora/` 或其他统一路径。

### 1.3 数据集文件（可选，可重新收集）

```
saved_from_server/datasets/
├── rlbench_raw/close_jar.hdf5       (598 MB) — 原始演示
└── rlbench_rlds/rlbench_close_jar/  (138 MB) — 已转换的 RLDS
```

如果不上传，可以在新服务器上重新收集和转换（见第 5、6 节）。

### 1.4 基础模型（可重新下载，推荐）

OpenVLA-7B 基础模型从 HuggingFace 下载（~15GB）：

```bash
huggingface-cli download openvla/openvla-7b --local-dir /root/checkpoints/openvla-7b
```

### 1.5 文件清单总结

| 文件 | 大小 | 必须？ | 目标路径 |
|------|------|--------|----------|
| `openvla-oft/` | ~50MB | 是 | `/opt/openvla-oft/` |
| `adapter_model.safetensors` | 675MB | 是 | `/root/checkpoints/close_jar_lora/` |
| `adapter_config.json` | 1KB | 是 | `/root/checkpoints/close_jar_lora/` |
| `close_jar.hdf5` | 598MB | 否 | `/opt/openvla-oft/datasets/rlbench_raw/` |
| `rlbench_close_jar/` (RLDS) | 138MB | 否 | `/opt/openvla-oft/datasets/rlbench_rlds/` |
| `openvla-7b/` (基础模型) | 15GB | 否 | `/root/checkpoints/openvla-7b/` |

---

## 2. Conda 环境配置

### 2.1 创建环境

```bash
conda create -n vggt-openvla-oft python=3.10 -y
conda activate vggt-openvla-oft
```

### 2.2 安装 PyTorch

根据 GPU 选择版本：

**A100/H100（推荐，支持 bf16）：**
```bash
pip install torch==2.2.0 torchvision==0.17.0 torchaudio==2.2.0 --index-url https://download.pytorch.org/whl/cu121
```

**V100 32GB（不支持 bf16，训练时需使用 `torch_dtype: "float16"` 或 `load_in_8bit: true`）：**
```bash
pip install torch==2.2.0 torchvision==0.17.0 torchaudio==2.2.0 --index-url https://download.pytorch.org/whl/cu121
```

### 2.3 安装项目依赖

```bash
cd /opt/openvla-oft
pip install -e .
pip install flash-attn==2.5.5 --no-build-isolation  # A100/H100 推荐
```

**V100 注意：** V100 不支持 flash-attn（Compute Capability 7.0），跳过此步。训练时设置 `load_in_8bit: true`。

### 2.4 安装 RLBench 相关依赖

```bash
pip install rlbench==1.2.0 pyrep==4.1.0.3
pip install tensorflow==2.15.0 tensorflow_datasets==4.9.3
pip install h5py scipy
```

### 2.5 验证安装

```bash
python -c "import torch; print(torch.__version__); print(torch.cuda.is_available())"
python -c "from rlbench import Environment; print('RLBench OK')"
```

---

## 3. RLBench + CoppeliaSim 安装

### 3.1 安装 CoppeliaSim V4.1.0

```bash
cd /opt
wget https://downloads.coppeliarobotics.com/V4_1_0/CoppeliaSim_Edu_V4_1_0_Ubuntu20_04.tar.xz
tar xf CoppeliaSim_Edu_V4_1_0_Ubuntu20_04.tar.xz
mv CoppeliaSim_Edu_V4_1_0_Ubuntu20_04 CoppeliaSim
```

### 3.2 设置环境变量

添加到 `~/.bashrc`：

```bash
export COPPELIASIM_ROOT=/opt/CoppeliaSim
export LD_LIBRARY_PATH=$LD_LIBRARY_PATH:$COPPELIASIM_ROOT
export QT_QPA_PLATFORM_PLUGIN_PATH=$COPPELIASIM_ROOT
```

### 3.3 安装 PyRep

```bash
pip install pyrep==4.1.0.3
```

### 3.4 安装 RLBench

```bash
pip install rlbench==1.2.0
```

### 3.5 验证

```bash
python -c "
import os
os.environ['COPPELIASIM_HEADLESS'] = '1'
from rlbench import Environment
from rlbench.action_modes import ActionMode
from rlbench.action_modes.arm_action_modes import EndEffectorPoseViaPlanning
from rlbench.action_modes.gripper_action_modes import Discrete
from rlbench.tasks import CloseJar

env = Environment(
    action_mode=ActionMode(EndEffectorPoseViaPlanning(), Discrete()),
    headless=True,
)
env.launch()
task = env.get_task(CloseJar)
print('RLBench + CoppeliaSim OK')
env.shutdown()
"
```

---

## 4. Headless 渲染修复

这是旧服务器上最大的坑。CoppeliaSim 在 headless 模式下需要 OpenGL 渲染上下文。

### 4.1 问题原因

- CoppeliaSim V4.1.0 自带 Qt 5.12 的 `libqxcobra.so`（xcb 平台插件）
- opencv-python（cv2）也自带了 Qt 5.15 的插件，会干扰 CoppeliaSim 的插件加载
- NVIDIA EGL 驱动与 Xvfb 虚拟显示不兼容：NVIDIA GLX 需要真实 GPU 显示，Mesa 需要通过软件渲染

### 4.2 解决方案

#### 步骤 1：安装必要的系统包

```bash
apt-get update
apt-get install -y xvfb mesa-utils libgl1-mesa-glx libgl1-mesa-dri libegl1-mesa
```

#### 步骤 2：创建 headless 启动脚本

创建 `/opt/start_xvfb.sh`：

```bash
#!/bin/bash
# Kill existing Xvfb
pkill Xvfb 2>/dev/null
sleep 1

# Start Xvfb with Mesa GLX support
export LIBGL_ALWAYS_SOFTWARE=1
Xvfb :99 -screen 0 1280x1024x24 +extension GLX +render -noreset &
sleep 2

export DISPLAY=:99
export COPPELIASIM_HEADLESS=1
export QT_QPA_PLATFORM=xcb

# Force Mesa for OpenGL (not NVIDIA)
export __GLX_VENDOR_LIBRARY_NAME=mesa
export MESA_GL_VERSION_OVERRIDE=3.3
export MESA_GLSL_VERSION_OVERRIDE=330

echo "Xvfb started on :99"
echo "DISPLAY=$DISPLAY"
```

#### 步骤 3：处理 cv2 Qt 插件冲突

```bash
# 用 CoppeliaSim 的 Qt 5.12 插件覆盖 cv2 自带的 Qt 5.15 插件
COPPELIA_QT_PLUGINS=/opt/CoppeliaSim/platforms
CV2_QT_PLUGINS=$(python -c "import cv2, os; print(os.path.join(os.path.dirname(cv2.__file__), 'qt', 'plugins', 'platforms'))")

if [ -d "$CV2_QT_PLUGINS" ]; then
    cp "$COPPELIA_QT_PLUGINS"/* "$CV2_QT_PLUGINS"/
    echo "Copied CoppeliaSim Qt plugins to cv2"
fi
```

#### 步骤 4：禁用 NVIDIA EGL（如有 NVIDIA GPU）

```bash
if [ -f /usr/share/glvnd/egl_vendor.d/10_nvidia.json ]; then
    mv /usr/share/glvnd/egl_vendor.d/10_nvidia.json \
       /usr/share/glvnd/egl_vendor.d/10_nvidia.json.bak
    echo "Disabled NVIDIA EGL vendor"
fi
```

#### 步骤 5：验证渲染

```bash
source /opt/start_xvfb.sh
python -c "
import os
os.environ['COPPELIASIM_HEADLESS'] = '1'
os.environ['DISPLAY'] = ':99'
from rlbench import Environment
from rlbench.action_modes import ActionMode, EndEffectorPoseViaPlanning, Discrete
env = Environment(ActionMode(EndEffectorPoseViaPlanning(), Discrete()), headless=True)
env.launch()
print('Headless rendering OK!')
env.shutdown()
"
```

### 4.3 备用方案：安装有 GPU 的服务器

如果 Mesa 软件渲染仍然失败，最直接的办法是用有物理 GPU + 显示器的服务器，或使用 NVIDIA Container Toolkit + EGL。

---

## 5. 数据收集

### 5.1 启动 Xvfb

每次运行 RLBench 前必须执行：

```bash
source /opt/start_xvfb.sh
conda activate vggt-openvla-oft
cd /opt/openvla-oft
```

### 5.2 收集 Evo-0 五个任务的演示数据

每个任务 100 个演示：

```bash
python experiments/robot/rlbench/collect_rlbench_demos.py \
    --tasks play_jenga put_knife_on_chopping_board \
        take_umbrella_out_of_umbrella_stand place_hanger_on_rack move_hanger \
    --demos_per_task 100 \
    --output_dir ./datasets/rlbench_raw \
    --overwrite
```

单任务收集（用于测试）：

```bash
python experiments/robot/rlbench/collect_rlbench_demos.py \
    --tasks close_jar \
    --demos_per_task 10 \
    --output_dir ./datasets/rlbench_raw \
    --overwrite
```

### 5.3 输出

每个任务在 `datasets/rlbench_raw/` 下生成一个 HDF5 文件，例如 `close_jar.hdf5`（~600MB/100 demos）。

---

## 6. 数据转换（HDF5 → RLDS）

### 6.1 转换命令

```bash
python experiments/robot/rlbench/convert_rlbench_to_rlds.py \
    --input_dir ./datasets/rlbench_raw \
    --output_dir ./datasets/rlbench_rlds \
    --tasks close_jar play_jenga put_knife_on_chopping_board \
        take_umbrella_out_of_umbrella_stand place_hanger_on_rack move_hanger
```

### 6.2 输出结构

```
datasets/rlbench_rlds/
├── rlbench_close_jar/
│   └── 0.1.0/
│       ├── dataset_info.json
│       ├── features.json
│       └── rlbench_close_jar-train.tfrecord-00000-of-00001
├── rlbench_play_jenga/
│   └── 0.1.0/
│       └── ...
└── ...
```

### 6.3 验证 RLDS 加载

```bash
python test_rlds_load.py --data_root_dir ./datasets/rlbench_rlds
```

---

## 7. 模型训练

### 7.1 GPU 和精度选择

| GPU | torch_dtype | LoRA | 备注 |
|-----|-------------|------|------|
| A100 40/80G | `bfloat16` | r=32 | 推荐配置 |
| H100 80G | `bfloat16` | r=32 | 推荐配置 |
| V100 32G | `float16` | r=32 | 不支持 bf16；用 `load_in_8bit: true` |

### 7.2 训练命令

**单任务训练（close_jar）：**

```bash
python vla-scripts/finetune.py \
    --vla_path /root/checkpoints/openvla-7b \
    --data_root_dir ./datasets/rlbench_rlds \
    --dataset_name rlbench_close_jar \
    --run_root_dir ./runs \
    --batch_size 4 \
    --learning_rate 2e-5 \
    --max_steps 250 \
    --save_freq 250 \
    --lora_rank 32 \
    --merge_lora_during_training False \
    --torch_dtype bfloat16 \
    --image_aug True \
    --use_proprio True \
    --num_images_in_input 2
```

**多任务训练（5 个 Evo-0 任务）：**

需要先合并多个 RLDS 数据集，或使用 `dataset_name` 为 `rlbench` 的统一配置。

### 7.3 关键参数说明

| 参数 | 说明 |
|------|------|
| `merge_lora_during_training` | **必须设为 `False`**（True 会在保存 checkpoint 时合并模型，需要 40GB+ CPU 内存导致 OOM） |
| `torch_dtype` | V100 用 `float16`，A100/H100 用 `bfloat16` |
| `load_in_8bit` | V100 建议设为 `True`，节省约 50% 显存 |
| `batch_size` | V100 建议 2-4，A100 建议 8 |
| `use_proprio` | RLBench 任务建议 `True`（使用本体感知状态） |
| `num_images_in_input` | 2 = 前摄像头 + 腕部摄像头 |

### 7.4 输出

训练输出在 `runs/<run_id>/` 下：
- `lora_adapter/adapter_model.safetensors` — LoRA 权重（训练结束后保存）
- `lora_adapter/adapter_config.json` — LoRA 配置
- `dataset_statistics.json` — 数据集统计（用于归一化）
- `training_metrics.csv` — 训练指标（如有启用）

---

## 8. 离线 LoRA 合并

训练时 `merge_lora_during_training=False`，所以需要离线合并。

### 8.1 合并脚本

```python
# merge_lora_offline.py
import torch
from transformers import AutoModelForVision2Seq, AutoProcessor
from peft import PeftModel

BASE_MODEL = "/root/checkpoints/openvla-7b"
LORA_ADAPTER = "./runs/<run_id>/lora_adapter"
OUTPUT = "./runs/<run_id>/merged_model"

print("Loading base model...")
base = AutoModelForVision2Seq.from_pretrained(
    BASE_MODEL,
    torch_dtype=torch.bfloat16,
    low_cpu_mem_usage=True,
)

print("Loading LoRA adapter...")
model = PeftModel.from_pretrained(base, LORA_ADAPTER)

print("Merging...")
model = model.merge_and_unload()

print(f"Saving to {OUTPUT}...")
model.save_pretrained(OUTPUT)

# Copy tokenizer/config files
import shutil, os
for f in ["tokenizer.model", "tokenizer_config.json", "special_tokens_map.json",
          "preprocessor_config.json", "processor_config.json"]:
    src = os.path.join(BASE_MODEL, f)
    if os.path.exists(src):
        shutil.copy2(src, os.path.join(OUTPUT, f))

# Copy action head and proprio projector from parent checkpoint
parent = os.path.dirname(LORA_ADAPTER)
for f in ["action_head.pt", "proprio_projector.pt"]:
    src = os.path.join(parent, f)
    if os.path.exists(src):
        shutil.copy2(src, os.path.join(OUTPUT, f))

print("Done!")
```

### 8.2 运行

```bash
python merge_lora_offline.py
```

合并后的模型在 `<run_id>/merged_model/`（~15GB），可直接用于评估。

---

## 9. 模型评估

### 9.1 评估命令

```bash
source /opt/start_xvfb.sh
conda activate vggt-openvla-oft
cd /opt/openvla-oft

python experiments/robot/rlbench/run_rlbench_eval.py \
    --pretrained_checkpoint ./runs/<run_id>/merged_model \
    --tasks '["close_jar"]' \
    --headless True \
    --num_episodes_per_task 25 \
    --image_size 256 \
    --use_proprio True \
    --num_images_in_input 2
```

### 9.2 参数说明

| 参数 | 说明 |
|------|------|
| `pretrained_checkpoint` | 合并后模型路径（或带 LoRA adapter 的 checkpoint 目录） |
| `tasks` | **JSON 列表格式**，如 `'["close_jar", "play_jenga"]'` |
| `num_episodes_per_task` | 每任务评估回合数（论文通常用 25 或 100） |
| `headless` | 必须 `True`（服务器无 GUI） |
| `use_proprio` | RLBench 必须 `True` |

### 9.3 评估结果

结果保存在 `./experiments/logs/EVAL-RLBENCH-<timestamp>.json`：

```json
{
  "close_jar": {"successes": 20, "total": 25, "success_rate": 0.8},
  "overall": {"successes": 20, "total": 25, "success_rate": 0.8}
}
```

### 9.4 无头评估的关键环境变量

每次评估前确保：

```bash
export DISPLAY=:99
export COPPELIASIM_HEADLESS=1
export QT_QPA_PLATFORM=xcb
export __GLX_VENDOR_LIBRARY_NAME=mesa
```

---

## 10. 常见问题

### Q1: `QXcbIntegration: Cannot create platform OpenGL context`

**原因：** NVIDIA GLX/EGL 与 Xvfb 虚拟显示冲突。

**解决：** 执行第 4 节的所有步骤，特别是设置 `__GLX_VENDOR_LIBRARY_NAME=mesa` 和禁用 NVIDIA EGL vendor。

### Q2: `Could not load Qt platform plugin "xcb"`

**原因：** cv2 自带的 Qt 5.15 插件与 CoppeliaSim 的 Qt 5.12 不兼容。

**解决：** 按 4.2 节步骤 3，将 CoppeliaSim 的平台插件复制到 cv2 路径。

### Q3: Checkpoint 保存时 OOM（被 kill）

**原因：** `merge_lora_during_training=True` 时，保存 checkpoint 需要把基础模型加载到 CPU 内存做合并（~40GB）。

**解决：** 已修复 — `merge_lora_during_training` 默认 `False`，训练后用第 8 节的方法离线合并。

### Q4: V100 训练时显存不足

**解决：**
- 设置 `load_in_8bit: true`（8-bit 量化）
- 减小 `batch_size` 到 2
- 确保 `torch_dtype: "float16"`（V100 无 bf16）

### Q5: `dataset_info.json not found`

**原因：** RLDS 数据集路径或命名不正确。

**解决：**
- 确保 `data_root_dir` 指向包含 `rlbench_<task>/0.1.0/dataset_info.json` 的目录
- 数据集名称在 `finetune.py` 中需要使用 `rlbench_<task>` 格式（如 `rlbench_close_jar`）

### Q6: 评估时 `action_head.pt not found`

**原因：** 合并后的模型目录缺少 action head 和 proprio projector。

**解决：** 从训练 checkpoint 目录复制这些文件到 merged_model（见 8.1 节末尾）。

---

## 附录 A：快速验证清单

新服务器设置完成后，按以下顺序验证：

1. [ ] `nvidia-smi` — GPU 可访问
2. [ ] `python -c "import torch; torch.cuda.is_available()"` — PyTorch CUDA
3. [ ] `source /opt/start_xvfb.sh` — Xvfb 启动成功
4. [ ] `python -c "from rlbench import Environment; ..."` — RLBench 可启动
5. [ ] 收集 1 个任务的 10 个演示（验证数据收集）
6. [ ] 转换为 RLDS（验证格式）
7. [ ] 训练 10 步（验证训练管道）
8. [ ] 离线合并 LoRA（验证合并）
9. [ ] 评估 5 个回合（验证推理管道）

## 附录 B：本地文件与服务器路径对照

| 本地路径 (`saved_from_server/`) | 服务器目标路径 |
|------|------|
| `runs/.../lora_adapter/*` | `/root/checkpoints/close_jar_lora/` |
| `datasets/rlbench_raw/close_jar.hdf5` | `/opt/openvla-oft/datasets/rlbench_raw/close_jar.hdf5` |
| `datasets/rlbench_rlds/rlbench_close_jar/` | `/opt/openvla-oft/datasets/rlbench_rlds/rlbench_close_jar/` |
