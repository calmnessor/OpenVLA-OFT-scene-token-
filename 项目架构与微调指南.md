# VGGT-Omega + OpenVLA-OFT 融合架构 & 微调指南

## 1. 云服务器信息

| 项目 | 详情 |
|------|------|
| 地址 | `ssh -p 24224 root@223.109.239.36` |
| 密码 | Aez9thee |
| 显卡 | Tesla V100-SXM2-32GB (Compute Capability 7.0, **不支持 bfloat16**) |
| Conda | `/home/vipuser/miniconda3`, 环境 `vggt-openvla-oft` |
| Python | 3.10, PyTorch 2.2.0+cu121 |

## 2. 目录结构

```
/root/
├── Afford+VLA/scene token+OpenVLA-OFT/
│   ├── openvla-oft/                    # ★ 主项目目录
│   │   ├── vla-scripts/
│   │   │   ├── finetune.py             # ★ 微调入口（核心修改文件）
│   │   │   ├── deploy.py               # 模型部署/推理
│   │   │   └── merge_lora_weights_and_save.py
│   │   ├── prismatic/
│   │   │   ├── models/
│   │   │   │   ├── scene_projector.py  # ★ VGGT场景token投影层
│   │   │   │   ├── action_heads.py     # 动作头
│   │   │   │   └── projectors.py       # 投影器
│   │   │   ├── extern/hf/
│   │   │   │   └── modeling_prismatic.py  # ★ OpenVLA模型定义(含scene_token处理)
│   │   │   └── vla/                    # VLA核心模块
│   │   ├── experiments/                # 实验工具
│   │   ├── runs/                       # ★ 训练输出目录(ckpt + CSV)
│   │   └── wandb/                      # WandB日志
│   ├── vggt-omega/                     # VGGT-Omega源码
│   ├── dlimp_openvla/                  # 数据预处理(RGB→RLDS)
│   └── transformers-openvla-oft/       # 修改版transformers
│
├── checkpoints/
│   ├── openvla-7b/                     # OpenVLA-7B预训练权重
│   └── sanqilu376/VGGT-Omega/
│       └── vggt_omega_1b_512.pt        # ★ VGGT-Omega权重(512分辨率)
│
└── datasets/modified_libero_rlds/      # LIBERO数据集(RLDS格式)
    ├── libero_spatial_no_noops/
    ├── libero_object_no_noops/
    ├── libero_goal_no_noops/
    └── libero_10_no_noops/
```

## 3. 模型架构

```
输入: 2个相机视角(主视角 + 手腕) + 语言指令
        │
        ├─── 224×224 图像 ──→ Vision Backbone (SigLIP/DinoV2)
        │                              │
        │                        视觉 patch tokens [B, N_patches, 1152]
        │
        └─── 512×512 图像 ──→ VGGT-Omega (❄️冻结 907M)
                                       │
                              camera_and_register_tokens [B, N, 17, 2048]
                                       │
                              register tokens [B, N*16, 2048]
                                       │
                              SceneProjector (可训练 ~8M)
                              Linear(2048→4096) + LayerNorm
                                       │
                              场景 tokens [B, N*16, 4096]
                                       │
        ┌────────── 视觉tokens + 场景tokens ──────────┐
        │                                              │
        ▼                                              ▼
  LLM Backbone (Llama-7B)  ←── LoRA (rank=32, 111M可训练)
        │
        ▼
  动作输出 (7维关节角度 × 8步chunk)
```

**关键维度：** VGGT输出 reg tokens: `[B, N, 16, 2048]` → flatten `[B, N*16, 2048]` → SceneProjector `[B, N*16, 4096]` → 拼接到 LLM 输入

**参数量：**
| 组件 | 参数量 | 状态 |
|------|--------|------|
| OpenVLA-7B | 7.66B | LoRA微调 1.45% (111M) |
| VGGT-Omega | 907M | ❄️ 冻结 |
| SceneProjector | ~8M | 全参数训练 |

## 4. 对 finetune.py 的关键修改

相对于原始 OpenVLA-OFT，做了以下修改：

1. **bfloat16 → float16**：V100 不支持 bf16，全局替换
2. **low_cpu_mem_usage=False**：避免 meta device 错误
3. **VGGTSceneExtractor 类**：加载 VGGT-Omega，提取 register tokens（跳过 camera_head/dense_head 的 128 个 key）
4. **SceneProjector 替换**：模型加载后强制替换为 scene_dim=2048 的正确版本（绕过 HF cache 问题）
5. **scene_tokens 传递链**：run_forward_pass → vla() → _process_scene_tokens() → LLM embeddings
6. **VGGT 图像预处理**：512×512 resize + center crop (ToTensor, 无 ImageNet 归一化)
7. **本地 CSV 日志**：训练指标自动保存到 `runs/<run>/training_metrics.csv`
8. **action_head=None 初始化**：不使用 L1 regression 时避免 UnboundLocalError

## 5. 当前数据集

LIBERO 4个子集（RLDS格式，2相机视角）：

| 数据集 | 描述 |
|--------|------|
| `libero_spatial_no_noops` | ★ 当前训练用：空间关系任务 |
| `libero_object_no_noops` | 物体交互任务 |
| `libero_goal_no_noops` | 目标导向任务 |
| `libero_10_no_noops` | 10任务混合 |

## 6. 启动微调

### 基础命令模板
```bash
ssh -p 24224 root@223.109.239.36
source /home/vipuser/miniconda3/etc/profile.d/conda.sh
conda activate vggt-openvla-oft
export WANDB_API_KEY=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
cd ~/Afford+VLA/scene\ token+OpenVLA-OFT/openvla-oft/

CUDA_VISIBLE_DEVICES=0 torchrun --standalone --nnodes=1 --nproc-per-node=1 \
  vla-scripts/finetune.py \
  --vla_path /root/checkpoints/openvla-7b \
  --data_root_dir /root/datasets/modified_libero_rlds/ \
  --dataset_name <数据集名> \
  --run_root_dir runs \
  --batch_size 1 \
  --grad_accumulation_steps 8 \
  --learning_rate 2e-5 \
  --image_aug True \
  --num_images_in_input 2 \
  --use_lora True \
  --lora_rank 32 \
  --max_steps <训练步数> \
  --save_freq <保存频率> \
  --use_l1_regression False \
  --use_proprio False \
  --merge_lora_during_training False \
  --use_scene_tokens True \
  --vggt_checkpoint /root/checkpoints/sanqilu376/VGGT-Omega/vggt_omega_1b_512.pt \
  --wandb_entity suziyang63-zzy \
  --wandb_project openvla-oft
```

### 参数说明

| 参数 | 含义 | 建议值 |
|------|------|--------|
| `--dataset_name` | LIBERO子数据集 | `libero_spatial_no_noops` |
| `--batch_size` | 每GPU batch | `1`（V100受限） |
| `--grad_accumulation_steps` | 梯度累积 | `8`（有效batch=8） |
| `--max_steps` | 最大训练步数 | `150000` |
| `--save_freq` | 保存checkpoint频率 | `10000` |
| `--use_scene_tokens` | 启用VGGT场景token | `True` |
| `--vggt_checkpoint` | VGGT权重路径 | 必须指定 |
| `--num_images_in_input` | 相机数量 | `2`（LIBERO固定） |
| `--merge_lora_during_training` | 保存时合LoRA | `False`（避免OOM） |
| `--use_l1_regression` | L1回归动作头 | `False`（节省显存） |

### 切换数据集
只需改 `--dataset_name`：
```
libero_spatial_no_noops   (当前)
libero_object_no_noops
libero_goal_no_noops
libero_10_no_noops
```

### 查看训练结果
- **在线**：浏览器打开 WandB 项目 https://wandb.ai/suziyang63-zzy/openvla-oft
- **本地CSV**：下载 `runs/<run_name>/training_metrics.csv`，用 pandas+matplotlib 画图
- **Checkpoint**：`runs/<run_name>/` 下每 save_freq 步保存的 LoRA adapter
