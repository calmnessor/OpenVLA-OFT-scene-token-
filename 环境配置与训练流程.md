# VGGT-Ω + OpenVLA-OFT 训练流程

## 环境配置

### 1. Conda 环境
服务器已预装 conda 环境：`/home/vipuser/miniconda3/envs/vggt-openvla-oft`

### 2. 安装依赖
```bash
conda activate vggt-openvla-oft
pip install git+https://github.com/kvablack/dlimp.git  # RLDS 数据集加载
```

### 3. 下载预训练权重
- **OpenVLA-7B**: ModelScope `sanqilu376/OpenVLA-7B` → `/root/checkpoints/openvla-7b/`
- **VGGT-Ω 1B**: ModelScope `sanqilu376/VGGT-Omega` → `/root/checkpoints/vggt_omega_1b_512/vggt_omega_1b_512.pt`

### 4. 下载 LIBERO 数据集
```bash
cd /root/datasets/rlds
# 下载 4 个任务集的 RLDS TFRecord 文件：
# libero_spatial_no_noops  (16 files, 1.8GB)
# libero_object_no_noops   (32 files, 2.7GB)
# libero_goal_no_noops     (16 files, 1.8GB)
# libero_10_no_noops       (32 files, 3.5GB)
```

## 关键架构

```
图片输入 (2 cameras × 256×256)
    ├──→ VGGT-Ω 1B (frozen) → scene_tokens [B, num_frames, 17, 1024]
    │         └── SceneProjector: Linear(1024→4096) + LayerNorm (~8M params)
    │
    └──→ OpenVLA-7B Vision Encoder (DINOv2 + SigLIP, frozen)
              │
              └──→ Llama-7B Backbone (LoRA rank=32, ~2.7M trainable)
                       │
                       └──→ Action Head: Linear(4096→56) → 8-step × 7-DOF 动作
```

## 训练命令

```bash
cd /root/openvla-oft

torchrun --standalone --nnodes 1 --nproc-per-node 1 \
  vla-scripts/finetune.py \
    --vla_path /root/checkpoints/openvla-7b \
    --data_root_dir /root/datasets/rlds \
    --dataset_name libero_spatial_no_noops \
    --use_l1_regression True \
    --use_scene_tokens True \
    --vggt_checkpoint /root/checkpoints/vggt_omega_1b_512/vggt_omega_1b_512.pt \
    --num_images_in_input 1 \
    --use_proprio True \
    --batch_size 2 \
    --grad_accumulation_steps 4 \
    --learning_rate 5e-4 \
    --lora_rank 32 \
    --max_steps 30000 \
    --wandb_entity "suziyang63-zzy" \
    --wandb_project "scene token + token"
```

## 关键参数说明

| 参数 | 值 | 说明 |
|------|-----|------|
| `num_images_in_input` | 1 | LIBERO 只有 2 相机（6 通道），必须设为 1 |
| `batch_size × grad_accum` | 2×4=8 | 有效 batch=8，用梯度累积省显存 |
| `learning_rate` | 5e-4 | LoRA 微调常用值 |
| `lora_rank` | 32 | 可训练参数 ~2.79M（总 7.6B 的 0.36%） |
| `max_steps` | 30000 | ~1 epoch，实际可按需调整 |
| `save_freq` | 10000 | 每 10K 步存一次 checkpoint |

## 常见坑

1. **必须用 `torchrun` 启动**，不能直接用 `python`（需要初始化分布式进程组）
2. **`num_images_in_input` 必须匹配相机数**：LIBERO=1，Bridge=3
3. **W&B entity** 需先 `wandb login` 确认正确用户名
4. **RLDS 数据集**需 `dlimp` 库（GitHub 安装，非 PyPI）
